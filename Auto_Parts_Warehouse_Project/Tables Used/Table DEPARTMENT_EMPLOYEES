CREATE TABLE DEPARTMENT_EMPLOYEES(
    EMPLOYEE_ID INT CONSTRAINT DEPT_EMP_FK1 REFERENCES EMPLOYEES(EMPLOYEE_ID),
    DEPARTMENT_ID INT CONSTRAINT DEPT_EMP_FK2 REFERENCES DEPARTMENTS(DEPARTMENT_ID),
    FROM_DATE DATE,
    TO_DATE DATE
);
