namespace Assets.ScriptFolder.PlayerScripts.Inventory.PlayerWeapons.Weapons.Bow.BowAmmo
{
    public class ArrowSpeedy : SubWeapon
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
