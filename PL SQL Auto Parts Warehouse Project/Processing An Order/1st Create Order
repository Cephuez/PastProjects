EXECUTE PACKAGE_ORDERS.CREATE_NEW_ORDER(3);
-- This will create a new order under and add it into the ORDERS Table

-- Procedure Reference
PROCEDURE CREATE_NEW_ORDER(C_ID INT)
IS
BEGIN
INSERT INTO ORDERS(ORDER_ID, CUSTOMER_ID)
VALUES(ORDER_ID_SEQ.nextval, C_ID); 
END;
